<script src="//vk.com/js/api/openapi.js" type="text/javascript"></script>
<!--div id="login_button" onclick="VK.Auth.login(authInfo);"> vklogin </div>
<script language="javascript"> 
    VK.init({   apiId: '4933339' }); 
    function authInfo(response) {   
        if (response.session) {     
        alert('user: '+response.session.mid);   
        } 
        else {     
            alert('not auth');   
        }
    } 
    VK.Auth.getLoginStatus(authInfo); 
    VK.UI.button('login_button');
</script-->

<!--fb:login-button show-faces="true" max-rows="1" size="large">
</fb:login-button>

<input type="button" ng-click="logout()" value="logout"-->




<div ng-app="geosearch">
    <div ng-controller="geosearchController" >

        <div class="centered">
            <input type="button" ng-click="send()" value="send">

            <table class="table table-striped table-bordered table-condensed">
                <tr ng-repeat="x in total">
                    <td> 
                    <input type="textarea" ng-model="$parent.firstId" ng-change="check()">
                    </td>
                        <td ng-repeat="y in chain" > 
                        </td>
                    <td>  
                    <input type="textarea" ng-model="$parent.secondId" ng-change="check()">
                    </td>
                </tr>
            </table>

        </div>
    </div>
</div>
